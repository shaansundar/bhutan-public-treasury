{
  "dependencies": {},
  "scripts": {
    "frontend": "cd frontend && next dev",
    "backend": "cd contracts && bunx hardhat node && bunx hardhat run deploy/index.ts --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.25.0",
    "@types/bun": "latest"
  },
  "name": "public-multisig",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
